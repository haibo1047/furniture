server:
  port: 10101

spring:
  application:
    name: configcenter
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

logging:
  level:
    '[com.example.demo]': debug